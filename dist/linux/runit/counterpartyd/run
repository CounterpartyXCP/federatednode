#!/bin/sh

USER=xcpd
export USER_HOME=/home/xcp
ulimit -n 4096
rm -f ${USER_HOME}/.config/counterpartyd/counterpartyd.*.db.lock
exec su -s /bin/bash -c '/usr/local/bin/counterpartyd --data-dir=${USER_HOME}/.config/counterpartyd server' ${USER}
